build/
vcpkg_installed/
generated/
compile_commands.json
*.o
*.obj

# Real config contains the geyser provider token and (from Phase 2 on) wallet
# key material -- never commit it. config.example.json is the tracked template.
config/config.json

# Hot wallet key material (Phase 2+)
*.key
*.keypair
wallet/

# Experiment output data
lag_samples.csv

# Generated report workbook -- rerun reports/generate_performance_report.py
# to regenerate; committing the binary would just be repeated diff noise.
reports/*.xlsx

# Python bytecode -- was accidentally tracked; regenerating it dirtied the tree
__pycache__/
*.pyc
